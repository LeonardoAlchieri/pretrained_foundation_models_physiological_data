defaults:
  - _self_
  - default@_here_: default
  - sweeps@_here_: basic
  - override dataset: apsync
  - override label_processor: binarizer

label_name: engagement # TODO: accepted: engagement
segment_length: 600
device_map: "cpu"

label_processor.threshold: 3.4999 # TODO: check if correct