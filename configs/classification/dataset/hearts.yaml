# defaults:
#   - label_processor: label_binarizer # binarizer for SleepQuality

dataset: HeartS
side: unknown
label_name: ${label_name}
label_processor: ${label_processor}
device_map: "mps"
threshold: 3
segment_length: 600 # seconds
sampling_rate: 4 # Hz
