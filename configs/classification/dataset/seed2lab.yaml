# defaults:
#   - label_processor: binarizer

dataset: SEED2Lab
side: unknown
label_name: ${label_name}
label_processor: ${label_processor}
device_map: "mps"
threshold: 4
segment_length: 10 # seconds
sampling_rate: 4 # Hz
