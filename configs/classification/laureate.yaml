defaults:
  - default
  - sweeps@_here_: basic
  - override dataset: laureate
  - override label_processor: label_binarizer
  - override resampling: none
  - _self_

label_name: Event Type # accepted: daily_caffeine, Event Type
segment_length: 60
device_map: "mps"

# label_processor:
#   threshold: 3 # TODO: check if correct

# # TODO: test if this is working!
# hydra:
#   sweeper:
#     grid_params:
#       label_name: lecture_calmness, lecture_energy, lecture_enthisiasm, lecture_motivation, lecture_peacefulness, lecture_satisfaction, lecture_stress, lecture_tiredness, lecture_understanding, lecture_unhappines, lecture_worriedness